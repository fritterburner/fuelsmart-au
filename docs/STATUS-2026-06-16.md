# FuelSmart AU — status briefing (2026-06-16, overnight)

Quick morning catch-up. Everything below is in the working tree, unpushed, verified (tsc clean; jest green bar the 3 pre-existing network-only fetcher tests; touched files lint clean).

## Built and ready (deploys on next push)
- **National coverage:** ACT live (rides NSW FuelCheck); VIC + SA adapters built, dormant behind env keys.
- **30-day history + 7-day forecast** (cycle + policy-event model, honest confidence/band).
- **Regional granularity:** ~50 named ABS-grounded hubs; `/history` region dropdown that defaults to the region nearest your last map position.
- **Detour-aware trip ranking**; **excise flagged off** (and the now-unused market-data cron is gated behind the same flag).
- **Per-station sparkline** in the map popup (30-day trend; reads `/api/history/station/[id]`).
- **Tap-average-here:** an "Area average" toggle on the map → tap to see the average + cheapest within 7 km for the selected fuel.
- **Compliance:** register up to date incl. the SA licence terms + ABS CC-BY credit line; pseudonymous usage counter + `/privacy`; **SA consumer-obligations component pre-built** (stale-price-to-CBS link + attribution, inert until SA is live).
- One-off **backfill endpoint** (shelved — see below).

## Blocked on something external (not code)
- **VIC + SA go-live** — waiting on the Servo Saver API key / SA publisher callback. On arrival: confirm the parser against a real response, set the env key. SA obligations UI is already built.
- **QLD backfill** — Vercel egress is geo-blocked by QLD's CDN. Pending the **Sydney-region experiment** (set the Vercel function region to `syd1` and re-try). NSW backfill was deliberately rejected (data too stale — would mislead the forecast). The nightly cron accrues everything forward regardless (~3 weeks to "medium" confidence).

## Parked (needs a decision, not blocked by us)
- **Native app / PWA + push price alerts** — the last real PetrolSmart gap.
- **Logbook** — deferred (low differentiation).

## To deploy this batch
1. *(optional)* apply `.gitattributes`: `git add --renormalize . && git commit`.
2. In Vercel: optionally set `BACKFILL_SECRET` / `USAGE_REPORT_SECRET`; leave `NEXT_PUBLIC_ENABLE_EXCISE` unset (excise stays off). History/usage reuse existing Upstash creds.
3. Commit + push (tsc-clean; Next 16 doesn't lint-gate). Ask Claude Code to commit/push (I can't push from here).

## Suggested next, when you're back
- Run the **Sydney-region** test → if QLD unblocks, backfill QLD's 6 months.
- Make the **native/PWA + push** decision (biggest remaining gap).
- Then it's polish: a live-data legend, per-region forecast surfacing on the map, etc.
